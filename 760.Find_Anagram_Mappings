class Solution(object):
    def anagramMappings(self, A, B):
        """
        :type A: List[int]
        :type B: List[int]
        :rtype: List[int]
        """
        rlist=[]
        for a in A:
            rlist.append(B.index(a))
        return rlist
        
