class Solution(object):
    def checkRecord(self, s):
        """
        :type s: str
        :rtype: bool
        """
        return True if (s.count('A')<=1 and s.count('L')<=2) or (s.count('A')<=1 and s.count('L')>2 and s.find('LLL')==-1) else False
        
